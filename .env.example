# Copia este archivo a .env.local y llena los valores.
# Proyecto Supabase: Axl-Projects (compartido con reels-analytics y admin-home).

NEXT_PUBLIC_SUPABASE_URL=https://impscwgourdxhdejwkhe.supabase.co

# Llave publicable (segura de exponer en el browser: RLS es lo que protege los datos).
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxx

# Solo para scripts de administracion (dar de alta admins). NUNCA se importa desde
# codigo que corra en el browser. Se saca de Supabase > Settings > API Keys.
SUPABASE_SECRET_KEY=
